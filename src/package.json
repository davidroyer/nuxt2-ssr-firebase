{
  "name": "nuxt-ssr-firebase-source",
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "lint": "eslint --ext .js,.vue --ignore-path .gitignore .",
    "precommit": "npm run lint"
  },
  "dependencies": {
    "@nuxtjs/pwa": "^2.0.8",
    "nuxt-edge": "^2.0.0-25452542.7f9d16c"
  },
  "devDependencies": {
  }
}
